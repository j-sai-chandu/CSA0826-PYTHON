numbers = list(map(int, input().split()))
seen = set()
duplicates = set()
for number in numbers:
    if number in seen:
        duplicates.add(number)
    else:
        seen.add(number)
print(list(duplicates))
